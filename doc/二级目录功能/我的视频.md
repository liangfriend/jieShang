# 我的视频

本地视频资源库，供作品编辑等功能选用。

## 能力

- **列表**：展示已上传视频；支持按名称搜索
- **添加**：选择本地视频（可多选）写入 `video` 表，文件落盘 `userData/resources/video`
- **删除**：软删库记录并删除磁盘文件
- **卡片播放**：播放 / 暂停 / 停止；进度条可拖拽
- **详情**：点击卡片查看时长、分辨率、大小、格式、添加时间

## 路由

- `/my-videos`（`myVideo`）
