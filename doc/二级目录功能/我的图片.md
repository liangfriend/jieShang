# 我的图片

本地图片资源库，供作品编辑等功能选用。

## 能力

- **列表**：展示已上传图片；支持按名称搜索
- **添加**：选择本地图片（可多选）写入 `image` 表，文件落盘 `userData/resources/image`
- **删除**：软删库记录并删除磁盘文件
- **预览**：卡片缩略图；点击查看尺寸、大小、格式、添加时间

## 路由

- `/my-images`（`myImage`）
