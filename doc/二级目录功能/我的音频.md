# 我的音频

本地音频资源库，供演唱模式等功能选择使用。

## 能力

- **列表**：展示已上传音频；支持按名称搜索
- **添加**：选择本地音频文件（可多选）写入 `audio` 表，文件落盘 `userData/resources/audio`
- **删除**：软删库记录并删除磁盘文件
- **卡片播放**：播放 / 暂停 / 停止；进度条可拖拽定位
- **详情**：点击卡片查看时长、采样率、声道、大小、格式、添加时间

## 路由

- `/my-audio`（`myAudio`）
