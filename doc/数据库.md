# 本地数据库（SQLite）

单机模式，无用户表。JSON 字段均以 **TEXT 字符串** 存储。

## score（曲谱）

仅存打谱数据，供曲谱编辑器读写。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | INTEGER PK | 自增 |
| name | TEXT | 曲谱名称 |
| data | TEXT | 曲谱 JSON 字符串（渲染插件格式） |
| thumbnail | TEXT | 曲谱缩略图（预留，可为空） |
| created_at | DATETIME | |
| updated_at | DATETIME | |
| deleted_at | DATETIME | 软删除 |

## work（作品）

在曲谱之上叠加画笔、媒体、音频等。曲谱本体在 `score` 表，通过 `score_id` 关联，便于从作品 **单独提取曲谱**。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | INTEGER PK | 自增 |
| name | TEXT | 作品名称 |
| score_id | INTEGER FK score(id) | 关联曲谱，可为空（草稿） |
| data | TEXT | 作品扩展 JSON，例：`{ brushStrokes, mediaResourceIds, audioResourceIds, layers }` |
| created_at | DATETIME | |
| updated_at | DATETIME | |
| deleted_at | DATETIME | 软删除 |

### 数据关系

```
score (曲谱 JSON)
  ↑ score_id
work (画笔 / 媒体 / 音频等 JSON)
```

业务说明见 [藏品.md](./藏品.md)。

## collection（藏品）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | INTEGER PK | 自增 |
| type | TEXT | 藏品类型：`tone_color` / `score_skin` / `piano_skin` / `perform_skin` |
| content | TEXT | 藏品内容。音色、曲谱皮肤为 JSON；钢琴皮肤为 SVG data URL；演奏皮肤为内置名称 |
| description | TEXT | 藏品说明，可为空 |
| is_built_in | BOOLEAN | 是否内置（软件自带），默认 false |
| owned | BOOLEAN | 是否拥有，默认 false |
| thumbnail | TEXT | 藏品缩略图，可为空 |
| created_at | DATETIME | |
| updated_at | DATETIME | |
| deleted_at | DATETIME | 软删除 |

### content 按类型

| type | content 含义 |
| --- | --- |
| tone_color | 音色 JSON，传入 NPlayer |
| score_skin | deciphony-renderer 皮肤 JSON |
| piano_skin | SVG data URL 字符串 |
| perform_skin | 内置演奏皮肤名称（PerformSkinNameList） |

## 迁移

| id | 说明 |
| --- | --- |
| 001-init | game、save、resource、group、migration |
| 002-score-courseware | 删除旧 work 表，创建 score、courseware（历史） |
| 003-courseware-to-work | 将 courseware 表重命名为 work |
| 004-score-thumbnail | score 表增加 thumbnail 字段（缩略图预留） |
| 005-collection | 创建 collection 表 |

## 保留表（游戏 / 资源）

见 `src/数据库新.md`：`game`、`save`、`resource`、`group`。
