# 藏品

## 概念

| 概念 | 说明 |
| --- | --- |
| 藏品类型 | 音色、曲谱皮肤、钢琴皮肤、演奏皮肤（见 `CollectionTypeEnum`） |
| 藏品表 `collection` | 本地已下载/入库的藏品记录，**不等于已拥有** |
| `owned` | 是否已拥有；**列表页只展示 `owned = true` 的记录** |
| `is_built_in` | 是否软件内置藏品；内置藏品不可删除，靠任务等方式解锁 `owned` |
| 社区藏品（二期） | 用户从网上下载的藏品，`is_built_in = false`，可删除；不含演奏皮肤（需 Vue 页面，暂不支持社区开发） |

## 数据分工

### 数据库 `collection` 表

存本地下载好的藏品实体与状态，字段见 [数据库.md](./数据库.md#collection藏品)。

| 字段 | 列表/详情用途 |
| --- | --- |
| `type` | 藏品类型 |
| `name` | 藏品名称 |
| `content` | 藏品本体：音色/曲谱皮肤/钢琴皮肤 JSON、演奏皮肤内置 key |
| `description` | 描述（社区藏品主要来源；内置可为空，走常量兜底） |
| `thumbnail` | 缩略图；无图时 UI 用 `?` 占位 |
| `is_built_in` | 是否内置；`true` 时禁止删除，并读取内置元数据 |
| `owned` | 是否拥有；未拥有不进入藏品列表 |

### 内置元数据 `src/renderer/src/constant/collection.ts`

仅存 **内置** 藏品的展示与解锁信息，不重复存 content 本体：

- `PerformSkinNameList`：演奏皮肤，`content` 存此处的 key（如 `default`、`RainBow`）
- `BUILTIN_COLLECTION_META`：按 `type → contentKey` 索引，可扩展音色/曲谱皮肤/钢琴皮肤

每条内置元数据：

```ts
{
  name: string       // 展示名称
  howToGet: string   // 获取条件（详情区「获取条件」）
  description?: string // 描述兜底
}
```

## 类型对照

| 渲染层 `CollectionTypeEnum` | 数据库 `type` |
| --- | --- |
| `toneColor` | `tone_color` |
| `scoreSkin` | `score_skin` |
| `virtualPianoSkin` | `piano_skin` |
| `performSkin` | `perform_skin` |

映射常量：`COLLECTION_ENUM_TO_DB` / `COLLECTION_DB_TO_ENUM`（`constant/collection.ts`）。

## 各类型 content 含义

| type | content |
| --- | --- |
| `tone_color` | 音色 JSON，传入 NPlayer |
| `score_skin` | deciphony-renderer 皮肤 JSON，传入 musicScore |
| `piano_skin` | 钢琴皮肤 JSON：按 midi(21~108) 分的皮肤包，每键 `{ normal, press, active }` 存 SVG data URL，可直接用于 `background-image` |
| `perform_skin` | 内置演奏皮肤名称（`PerformSkinNameList` 的 key） |

## 列表页逻辑（`CollectionView.vue`）

1. 调用 `window.api.collection.query({ owned: true })` 拉取已拥有藏品。
2. 顶栏：名称模糊搜索（`includes`，不区分大小写）。
3. 左侧主区域：类型筛选（`全部` + 遍历 `CollectionTypeEnum`）+ 卡片网格列表。
4. 右侧窄栏详情：缩略图（无则 `?`）、名称、类型、描述、获取条件。
4. **名称**：优先数据库 `name`；内置兜底 `BUILTIN_COLLECTION_META` → `description` 首行 → 类型 + id。
5. **描述**：优先数据库 `description`；内置可为空时读常量 `description`。
6. **获取条件**：仅 `is_built_in === true` 时，从 `getBuiltinMeta(type, content).howToGet` 读取，展示在描述下方。
7. **删除**：仅 `is_built_in === false` 显示删除按钮，调用 `collection:delete`。

## 工具函数

`src/renderer/src/utils/collection/collectionHelper.ts`：

- `fetchOwnedCollections` — 列表数据
- `resolveCollectionName` / `resolveCollectionDescription` / `resolveCollectionHowToGet` — 展示字段
- `canDeleteCollection` — 是否可删
- `deleteCollectionFromDatabase` — 删除社区藏品

## 内置种子数据

迁移 `001-init` 的 `up`：建表后写入内置藏品（随 migration 只执行一次）：

**演奏皮肤**

| name | content | is_built_in | owned（测试期） |
| --- | --- | --- | --- |
| 默认演奏皮肤 | `default` | true | true |
| 彩虹演奏皮肤 | `RainBow` | true | true |

**钢琴皮肤**（`src/main/resources/virtualPianoSkins/builtinSkins.ts` 按白键/黑键模板循环生成 88 键，`JSON.stringify` 入库）

| name | is_built_in | owned（测试期） |
| --- | --- | --- |
| 经典纯色 | true | true |
| 金属质感 | true | true |

## 当前使用的藏品（localStorage）

键名：`collection-active-selection`

```ts
{
  toneColor?: { id: number; name: string }
  scoreSkin?: { id: number; name: string }
  virtualPianoSkin?: { id: number; name: string }
  performSkin?: { id: number; name: string }
}
```

- 各类型统一存 **id + name**
- 首次启动无缓存：`initDefaultCollectionSelection()` 查数据库补全并写入 localStorage
- **演奏皮肤**：`pianoMidiBox` / `pianoWaterfall` 用 `id` 查库取 `content`，再以 `content` 索引 `performSkin/registry` 本地包
- **钢琴皮肤**：`virtualPiano` 用 `id` 查库取 `content`（VirtualPianoPack）渲染

工具：`collectionActiveStorage.ts`、`initCollectionSelection.ts`、`performSkinLoader.ts`、`virtualPianoSkinLoader.ts`

## 演奏皮肤本地结构（performSkin）

一套皮肤 = 7 部分，目录 `src/renderer/src/components/performSkin/{name}/`：

| 部分 | 文件 |
| --- | --- |
| midi 块普通态 | `midiBox/normal.ts` |
| midi 块激活态 | `midiBox/active.ts` |
| 瀑布流普通水柱 | `waterfall/normal.ts` |
| 瀑布流激活水柱 | `waterfall/active.ts` |
| 背景 | `svg.ts` → `bg` |
| 基准线 | `svg.ts` → `baseline` |
| 基准线 midi 按下高亮 | `svg.ts` → `baselineMidiActive` |

**基准线 midi 按下高亮**：`svg.ts` 提供 `baselineMidiActive`；`pianoWaterfall` / `pianoMidiBox` 内按 `activeKeys` 合并 `keyActiveBar` 布局与 SVG 背景（宽度传 `getMidiWidth(midi)` 数值，单位 px）。

注册表：`performSkin/registry.ts`（`content` 与文件夹对应）

| content | 文件夹 | 说明 |
| --- | --- | --- |
| `default` | `default/` | 普通黄、激活蓝、白底、红基准线 |
| `RainBow` | `rainbow/` | 原彩色糖果风（自 pianoMidiBox / pianoWaterfall 抽出） |

演奏皮肤 **不** 把样式 JSON 存数据库，只存 `content` 名称；样式在代码库维护。社区功能不支持用户自制演奏皮肤。

## 二期规划（社区）

- 允许用户下载除 **演奏皮肤** 外的藏品（演奏皮肤依赖内置 Vue 组件）。
- 下载入库：`is_built_in = false`，`owned` 由购买/下载流程置 `true`。
- 用户可在列表删除自己下载的藏品；内置藏品始终不可删。
